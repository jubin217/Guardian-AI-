# Core - Relaxed for Pi
numpy<2.0.0
scipy>=1.10.0
requests>=2.28.0
idna>=3.4

# PyTorch - On Pi, we often rely on system packages or specific wheels
# If installing via pip, these might take a long time to build on ARM if wheels aren't found
torch>=2.0.0
torchaudio>=2.0.0
torchvision>=0.15.0

# Audio / Speech
sounddevice>=0.4.6
pyaudio>=0.2.13
pydub>=0.25.1
librosa>=0.10.0
speechrecognition>=3.10.0
# whisper might require rust to build, be patient
openai-whisper
faster-whisper
speechbrain
pyttsx3>=2.90

# NLP
transformers
langdetect
deep-translator

# CV / Face
# Use opencv-python-headless for servers, but this app has cv2.imshow so we need standard
opencv-python>=4.8.0
deepface
mtcnn
retina-face

# Backend
Flask>=3.0
flask-cors>=4.0
gunicorn>=20.0

# Utilities
tqdm
psutil
rich
ultralytics
vosk
